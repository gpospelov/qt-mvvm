language: cpp
sudo: required

matrix:
  include:
    - os: linux
      services: docker
      env: QT_QPA_PLATFORM=offscreen BUILD_TYPE=Release TESTS_AUTODISCOVERY=OFF CMAKE_DIRNAME="cmake_install"
    - os: osx
      osx_image: xcode11.3
      compiler: clang
      env: BUILD_TYPE=Release TESTS_AUTODISCOVERY=OFF

install:
- .travis/${TRAVIS_OS_NAME}/install.sh

script:
- .travis/${TRAVIS_OS_NAME}/build.sh
